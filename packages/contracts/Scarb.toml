[package]
name = "phantom_darkpool_contracts"
version = "0.1.0"
edition = "2023_11"

[dependencies]
starknet = ">=2.5.0"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"
build = "scarb build"
